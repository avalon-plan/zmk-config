---
include:
  # Super52840 = XIAO nRF52840 替代品(相同引脚 + XIAO Bootloader)
  - board: seeeduino_xiao_ble
    shield: knob_macropad
    snippet: studio-rpc-usb-uart   # 启用 ZMK Studio;调 AGC 时临时换成 zmk-usb-logging
